# ── Web 框架 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.0.0
python-multipart>=0.0.9

# ── LLM ──
anthropic>=0.30
openai>=1.30

# ── 存储 ──
chromadb>=0.5.0
pymupdf>=1.24.0
pypdf>=4.0.0

# ── Embedding & NLP ──
sentence-transformers>=3.0.0
spacy>=3.7

# ── 配置 & 工具 ──
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
schedule>=1.2
portalocker>=2.8
numpy>=1.24

# ── 可选：高质量 PDF 解析（任选其一）──
# magic-pdf          # MinerU: pip install magic-pdf
# marker-pdf         # Marker: pip install marker-pdf
